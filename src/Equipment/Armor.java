package Equipment;

public class Armor extends Equipment{

    public Armor(String itemName, int reqLevel, int itemLevel, String itemType, String itemSlot, boolean isWeapon, boolean isArmor) {
        super(itemName, reqLevel, itemLevel, itemType, itemSlot, isWeapon, isArmor);
    }
}
